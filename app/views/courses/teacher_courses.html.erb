<h1 class="title">my courses</h1>
 <p><%=link_to "Back to Home Page", root_path, class: "back_button"%></p>

<div class="actions">
    <%= link_to "Add A Course", new_course_path, class: "back_button" %>
</div>

<div class="container">
  <div class="description">
    <% @courses.each do |c| %>
      <%if current_teacher.id == c.teacher_id%>
      <p><%=link_to c.name, teacher_course_path(c), class: "description"%> </p>
    <% end end %>
  </div>
</div>

